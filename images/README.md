# Images

Place your dashboard screenshot here as `dashboard.png`.

**How to capture it:**
- Build the Power BI dashboard using `docs/power_bi_build_guide.md`, then export/screenshot it, OR
- Open `report/index.html` in a browser and screenshot it as an interim visual.

The main `README.md` references `images/dashboard.png`.
